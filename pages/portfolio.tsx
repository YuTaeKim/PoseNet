import Layout from 'components/Layout';

const IndexPage = () => (
  <Layout title="로켓메이커스">
    <div
      style={{
        alignItems: 'center',
        justifyContent: 'center',
        display: 'flex',
        flex: 1,
      }}
    >
      <h1>포트폴리오 입니다.</h1>
    </div>
  </Layout>
);

export default IndexPage;
