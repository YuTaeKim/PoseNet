import Layout from 'components/Layout';

const IndexPage = () => (
  <Layout title="로켓메이커스">
    <div
      style={{
        alignItems: 'center',
        justifyContent: 'center',
        display: 'flex',
        flex: 1,
      }}
    >
      <h1>로켓메이커스 소식입니다.</h1>
    </div>
  </Layout>
);

export default IndexPage;
